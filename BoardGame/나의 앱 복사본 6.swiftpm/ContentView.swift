import SwiftUI

struct ContentView: View {
    
    var body: some View {
        VStack {
            BoardView(character: character1, boardSet: boardSet1)
            //MoveButton()
        }
        
    }
}
